{
  "name": "tau-desktop",
  "private": true,
  "version": "0.3.1",
  "description": "gzTau desktop shell — bundled public UI + loopback API (product repo only)",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "package": "tauri build && node ./scripts/copy-installer.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
