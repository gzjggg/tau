{
  "name": "tau-desktop",
  "private": true,
  "version": "0.1.1",
  "description": "Tau desktop shell — discovers Pi/Tau instances and opens a native window",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
